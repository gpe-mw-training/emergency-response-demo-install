---

- name: Deploy Istio Operator
  hosts: localhost
  connection: local
  gather_facts: false
  run_once: true
  vars_files:
    - group_vars/istio.yml

  tasks:
    - set_fact:
        namespace: "{{ namespace_istio_operator }}"
        resources_dir: "{{ resources_base_dir }}/istio"
        work_dir_name: istio-operator
    - include_role:
        name: openshift_istio_operator
      when: >
        ACTION is not defined or
        ACTION is none or
        ACTION|trim() == "" or
        ACTION|trim() == "install"
    - include_role:
        name: openshift_istio_operator
        tasks_from: uninstall
      when: >
        ACTION is defined and
        ACTION|trim() == "uninstall"