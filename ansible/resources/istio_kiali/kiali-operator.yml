---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ kiali_operator_deployment }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kiali-operator
      version: v1.0.0
  template:
    metadata:
      name: kiali-operator
      labels:
        app: kiali-operator
        version: v1.0.0
    spec:
      serviceAccountName: {{ kiali_operator_serviceaccount }}
      containers:
      - name: ansible
        command:
        - /usr/local/bin/ao-logs
        - /tmp/ansible-operator/runner
        - stdout
        image: {{ kiali_operator_image }}
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - mountPath: /tmp/ansible-operator/runner
          name: runner
          readOnly: true
      - name: operator
        image: {{ kiali_operator_image }}
        imagePullPolicy: IfNotPresent
        volumeMounts:
        - mountPath: /tmp/ansible-operator/runner
          name: runner
        env:
        - name: WATCH_NAMESPACE
          value: ""
        - name: POD_NAME
          valueFrom:
            fieldRef:
              fieldPath: metadata.name
        - name: OPERATOR_NAME
          value: "kiali-operator"
      volumes:
      - name: runner
        emptyDir: {}
